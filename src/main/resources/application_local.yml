spring:
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 3         # Neon 무료 티어 연결 제한 고려
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000